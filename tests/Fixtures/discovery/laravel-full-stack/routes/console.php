<?php

// Fixture placeholder only — read as plain text by Project Discovery,
// never included/required/executed.

use Illuminate\Support\Facades\Schedule;

Schedule::command('fixture:noop')->daily();
